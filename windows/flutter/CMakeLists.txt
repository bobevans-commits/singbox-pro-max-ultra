# Generated file, do not edit.
#

cmake_minimum_required(VERSION 3.14)

set(FLUTTER_MANAGED_DIR "${CMAKE_CURRENT_SOURCE_DIR}/flutter")

# Build configuration follows the Flutter tooling.
# This file should not be checked into version management.

# Load the Flutter build configuration.
include("${FLUTTER_MANAGED_DIR}/ephemeral/flutter_generated.cmake" OPTIONAL)

# Configure the build.
# This is a placeholder that will be populated by the Flutter tooling
# at build time with the correct engine version, etc.
